# CLAUDE.md — K-MODEL PORTFOLIO 배틀판 (TDY vs CLD)

> 문서 버전: v0.4-draft (2026-08-13) — 개정 3호: **TDY 수시 입력 체제.** TDY는 발생 시마다 미러 입력(거의 매일 가능), CLD는 주간 유지. 오염 차단 강화(세션 분리·팀더윤쎈 검색 금지). 개정 2호(배틀 프로토콜)·1호(국고채 ETF 듀레이션 보유) 유지.
> 지위: 본 프로젝트의 단일 규범(SSOT). 대화 중 합의된 변경은 이 문서에 반영되기 전까지 효력 없음.
> [ ] 안의 숫자는 예시 기본값이며, 확정치는 config/params.yaml에서만 관리한다.

---

## 0. 정체성과 관할 경계

- 목적: 팀더윤쎈 6단계 방법론을 **동일하게** 적용하되 각 단계의 '판단'을 Claude가 독립 수행하는 모델 포트폴리오(**CLD**)를 운용하고, 팀더윤쎈 공개 모델포트(**TDY**)·KOSPI·복합BM과 성과를 비교한다.
- 한 줄 요약: **같은 방법론, 다른 두뇌. 누가 더 잘하나 측정한다.**
- 사이클: 주간 평가(매주 [금요일 장마감 후]), 매매는 밴드 이탈 시에만.
- 자산 구성: 주식(개별종목 바스켓) + **국고채 ETF**(비주식 파트 — 현금 대기가 아니라 듀레이션을 실제 보유).
- 관할 분리(3권 분립): 분석헌법=「무엇을」 / 매매헌법=「언제·얼마나」 / **본 문서=모델 포트폴리오 배틀 트랙**. 실계좌·기존 헌법 체계와 격리. 통합은 사용자가 명시 지시할 때만, `input/` 인터페이스를 통해서만.
- 역할:
  - **Claude**: CLD 트랙의 **단독 자율 운용역** (페이퍼 한정) + 엔지니어. 하드캡 안에서 판단을 스스로 확정하고 전부 근거를 기록한다. 실계좌에 대한 "추천" 표현은 금지 — 모든 산출은 "CLD 모델 산출"로만 표기.
  - **운용자**: 심판 + 기록관. TDY 입력, 파라미터 관리, 결과 검증. **CLD 판단에 개입하지 않는다** — 개입하는 순간 '클로드 vs 팀더윤쎈'이 아니라 '사람+클로드 vs 팀더윤쎈'이 되어 실험이 오염된다.

## 1. 절대 규칙 (HARD RULES — 위반 시 출력 중단·사유 보고, 배틀 규칙 위반 시 해당 주차 기록 무효)

1. **숫자 날조 금지.** 모든 수치는 출처 + 기준일(asof) 병기. 확보 실패 시 `N/A(사유)`.
2. **소스 상충 시 병기.** 값들을 나란히 보이고 채택 기준을 명시한다.
3. **룩어헤드 금지.** 판단 시점 이전에 알 수 있었던 데이터만 사용. 컨센서스류는 발표 시차를 보수적으로 가정.
4. **오염 금지 (배틀 핵심).** ① CLD 판단(STEP 2~4) 중 `portfolios/tdy.json` 로드 금지, **팀더윤쎈 관련 게시물·채널·계정의 검색·열람도 금지**(웹검색 포함). ② CLD 평가일에는 `CLD 확정 → ledger 기록` 이후에만 `/tdy-input` 허용. ③ TDY 입력 세션과 CLD 리밸 세션은 분리한다 — 같은 세션에서 `/tdy-input` 후 `/weekly` 연속 실행 금지(역순은 허용).
5. **동일 조건 (배틀 핵심).** CLD·TDY 모두 같은 체결가(평가일 익영업일 시가), 같은 거래비용, 같은 TR 회계를 적용한다.
6. **가드레일 위반 포트폴리오 출력 금지.** §2 한도 초과 결과는 출력하지 말고 어떤 한도에 걸렸는지 보고.
7. **파라미터 하드코딩 금지.** 모든 숫자는 `config/params.yaml`에서만.
8. **미기록 변경 금지.** 상태 변경은 `state/` 갱신 + `CHANGELOG.md` 기록 동반. ledger는 **append-only** — 과거 원장·리포트 사후 수정 금지.
9. **침묵 실패 금지.** 수집·계산 실패는 리포트 최상단 ⚠️ 배너로 노출.

## 2. 파이프라인 (6단계, CLD 트랙)

### STEP 1 — SAA: Yield Gap 모형 (순수 계산)
- 수식: `EY = 1 / KOSPI 12M Fwd PER`, `BY = 국고채 10년`, `기본 주식비중 = EY / (EY + BY)`
- 안전자산 파트: **국고채 ETF 보유**(비중 = 1 − 주식비중). 기본 10년물 추종(`bond_etf_ticker`) — Yield Gap의 BY와 듀레이션 축을 일치시켜 모형–실물 정합 유지.
- `bond_mode`: `full`(10년물 고정, 기본) / `regime_switch`(STEP 2 한은 스탠스 −1이면 단기물로 듀레이션 축소, CHANGELOG 기록 필수).
- 입력: Fwd PER 1순위 컨센서스 소스, 실패 시 트레일링 PER 폴백(리포트에 "트레일링 대체" 명시). 국고10년은 ECOS 최우선.
- 강화 규칙: ① 산출 주식비중 [40%, 85%] 클리핑(원산출값 병기) ② Yield Gap [5년] z-score 병기 ③ 극단값 프로토콜 — Fwd PER < [7] 또는 > [16]이면 EPS 추정 최근 [1개월] 변동률 점검 + 트레일링 기준 산출 병기.

### STEP 2 — TAA: 스코어카드 + Claude 재량 (판단)
- `TAA = SAA ± View`, 총 틸트 하드캡 **±[5]%p**, 최종 TAA도 [40%, 85%] 캡 내.
- **앵커 = 스코어카드** (각 −1/0/+1): ①수출증가율 YoY ②원/달러 [20일] 추세 ③외국인 KOSPI [20일] 누적 순매수 ④국고 3-10년 스프레드 ⑤한은 정책 스탠스.
- 매핑: [−5..−3]→−5%p / [−2..−1]→−2.5%p / 0→0 / [+1..+2]→+2.5%p / [+3..+5]→+5%p.
- **Claude 재량 오버라이드 (배틀 계측기):** Claude는 스코어카드 매핑값과 다른 틸트를 자율 적용할 수 있다. 단 ① 총 캡 ±5%p 내 ② `재량 사유` 3줄 이상 로그 필수 ③ `틸트_스코어카드`와 `틸트_최종`을 분리 기록 — 이 차이의 손익이 **'재량 기여도'**로 귀속된다. 클로드 머리가 기계보다 나은지 여기서 숫자로 판명된다.
- `bond_mode=regime_switch` 시 #5 판정은 채권 듀레이션 전환에도 연동.

### STEP 3 — 섹터 배분 (판단)
- KOSPI 섹터 시총 비중(BM) 대비 액티브 편차. 한도: 섹터당 ±[7]%p, 단일 섹터 절대 상한 [35]%.
- OW/UW 결정마다 근거 데이터(상대강도·수급·이익수정비율 등) 출처 병기. 정성 판단 허용, 무근거 금지.

### STEP 4 — 종목 선정: Bottom-up (판단)
- 유니버스: [TDY와 동일 범위 — 확인 전 기본값 KOSPI200 + KOSDAQ150]. 유동성: [20일] 평균 거래대금 ≥ [50억]. 관리·거래정지 제외.
- 한도: 종목 수 [10~20], 종목당 상한 [10]%, 최소 편입 [2]%, 상위 5종목 합 ≤ [45]%.
- **선정 카드** 필수: ①투자논리 3줄 ②반대논리 1줄 ③무효화 조건(깨지면 편출) ④편입 기준일·기준가.
- 외부 스크리닝 훅(`input/candidates.csv`) 허용하되 로직은 본 문서에 편입 금지(트랙 오염 방지). **배틀 모드에서는 후보 제공만 가능하며 최종 선정·비중은 Claude 자율.**

### STEP 5 — 벤치마크 대비 균형 (계산)
- 매 산출: 액티브 셰어, 섹터 편차 테이블, 상위 집중도.
- 이중 벤치마크: ①KOSPI 단독 ②복합BM = KOSPI×주식비중 + KAP 국고채10년(TR)×채권비중.
- 편차 상한 초과 시 STEP 3~4로 1회 회귀, 재초과 시 사유 명기 후 진행.

### STEP 6 — 리밸런싱 (계산)
- 주간 평가는 매주, **매매는 목표비중 ±[2]%p 밴드 이탈 종목·채권 ETF에 한해서만** (양방향 드리프트 — 채권 ETF는 금리만으로도 이탈 가능).
- 체결: 평가일 **익영업일 시가.** 왕복 비용 [0.3]% 반영. 분배금 즉시 재투자, TR 회계.
- `ledger/YYYY-MM-DD.json` append-only 기록.
- 귀속 분해: 주식 종목 / 채권 파트 / 배분(비중) / **재량(오버라이드)** 기여를 분리 보고.

## 3. TDY 트랙 (수시 입력 전용)

- TDY는 **발생 시마다** 미러링한다(거의 매일 가능). 운용자가 `/tdy-input`으로 변경 내역(종목·비중·**변경일**)을 입력한다.
- 지연 입력 허용: `change_date`(TDY 실제 변경일)와 `input_date`(기록일)를 분리 저장하고, **체결은 항상 change_date의 익영업일 시가** 기준으로 소급 평가한다.
- CLD 평가일 당일에는 해당 주차 CLD ledger 기록 이후에만 입력 가능(HARD 4-②).
- TDY도 §1-5의 동일 체결·비용·TR 규칙으로 평가 — TDY의 매매 빈도가 높을수록 비용도 그대로 누적된다(배틀 관전 포인트). 원본 비중과 시스템 평가 비중이 다르면 병기한다.
- CLD는 주간 사이클을 유지한다. 케이던스 비대칭(TDY 수시 vs CLD 주간)은 결함이 아니라 **비교 대상 그 자체**다: 잦은 대응 vs 주간 규율.

## 4. 데이터 소스와 폴백

| 항목 | 1순위 | 폴백 | 실패 시 |
|------|-------|------|---------|
| 시세·지수·섹터·수급 | pykrx (KRX) | KRX 정보데이터시스템 수동 | N/A + 배너 |
| 국고채 10년 | ECOS Open API | 금융투자협회 수기 | N/A + 배너 |
| 12M Fwd PER / EPS 컨센서스 | 지정 크롤 소스(확정 필요) | 트레일링 PER (pykrx) | 트레일링 대체 명시 |
| 재무·공시 | DART Open API | — | N/A + 배너 |
| 국고채 ETF 시세·분배금 | pykrx / 운용사 공시 | KRX 정보데이터시스템 | N/A + 배너 |

- 캐싱: `data/cache/YYYYMMDD/` 원본 저장, 당일 중복 호출 금지. API 키는 `.env` 전용.

## 5. 파일 구조와 상태 관리

```
k-model-portfolio/
├── CLAUDE.md                # 본 문서 (SSOT)
├── CHANGELOG.md
├── config/params.yaml       # 모든 숫자의 유일한 서식지
├── .env
├── data/{cache,raw}/
├── input/candidates.csv     # (선택) 외부 스크리닝 훅
├── src/                     # saa.py taa.py sector.py screen.py rebalance.py report.py datafeed.py
├── state/                   # cld 산출 상태 (saa/taa/sector/picks)
├── portfolios/              # cld.json, tdy.json
├── ledger/                  # 주차별 거래 원장 (append-only)
├── reports/YYYY-MM-DD.md
└── scoreboard.md
```

- `portfolios/*.json` 핵심 스키마: `version, asof, weights{}, params_hash, last_rebalance`. `params_hash` 불일치 시 리포트 경고.

## 6. 커맨드 (.claude/commands/)

- `/weekly` — CLD 6단계 전체 실행 → 리포트 → state·ledger 갱신
- `/tdy-input` — TDY 변경 수시 입력 (change_date 필수, HARD 4 순서 준수)
- `/score` — 스코어보드 갱신 (CLD vs TDY vs KOSPI vs 복합BM)
- `/yieldgap` `/macro` `/screen [섹터]` — 개별 STEP 갱신
- `/audit` — 월 1회 정합성 감사: state ↔ 리포트 ↔ CHANGELOG ↔ ledger 4자 대조 + 오염 규칙(HARD 4) 위반 여부 점검

## 7. 스코어보드 (`scoreboard.md`)

- 누적/주간 수익률: **CLD vs TDY vs KOSPI vs 복합BM**
- MDD, 변동성, 주간 승률(vs TDY / vs KOSPI), **트랙별** 턴오버·누적 비용(매매 빈도 비교)
- **View 채점**: 지난주 틸트 방향 적중 여부 1줄 (스코어카드 vs 재량 각각)
- 월 1회 기여도 분해: SAA / 틸트(스코어카드) / **재량 오버라이드** / 섹터 / 종목 / 채권 — 어느 두뇌 파트가 벌고 잃는가.

## 8. 코딩 규칙

- Python. 모든 데이터 함수는 값 + `asof` + `source` 반환. 예외는 배너로 승격.
- 계산 함수는 순수 함수(입력→출력), 부수효과(state·ledger 쓰기)는 rebalance.py에만.
- 신규 숫자는 params.yaml 추가 후 참조.

## 9. 주간 리포트 템플릿 (골격)

```
# 주간 배틀 리포트 — {날짜}
⚠️ 데이터 경고: {있으면}
1. SAA: Fwd PER {}(asof,출처) | 국고10Y {} | EY {} | Yield Gap {}%p (z={}) | 기본 주식비중 {}%
2. TAA: 스코어카드 {점} → {}%p | 재량 오버라이드 {}%p (사유 요약) → 최종 주식 {}% / 국고채 ETF {}% (듀레이션 {}년, ±100bp ±{}%)
3. 섹터: BM 편차 테이블 + 변경 근거
4. 종목: 구성 + 선정 카드 변경분
5. 리밸런싱: 실행 여부, 매매 목록, 밴드 이탈 사유
6. 배틀 현황: 주간·누적 CLD vs TDY vs KOSPI, 승률, MDD
7. 다음 주 점검 포인트
```

## 10. 개정 절차 및 초기 셋업

- 본 문서·params.yaml 개정은 CHANGELOG 기록 + 버전 증가로만 유효. 대화 중 합의는 반영 전까지 규범 아님.
- 월 1회 `/audit` 후 규칙–실무 괴리 발견 시 둘 중 하나를 명시적으로 고친다.
- **초기 셋업**: 이 문서를 스펙으로 `src/`·커맨드를 구현하고, pykrx·ECOS 수집 검증 리포트 출력 후 첫 `/weekly`를 실행한다. Fwd PER 크롤 소스는 셋업 시 확정하여 §4 표를 갱신한다.
